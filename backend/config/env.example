# ============================================
# VoltWay - Configurações de Ambiente
# ============================================

# Configurações do Servidor
PORT=3000
NODE_ENV=development

# Configurações do Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=voltway
DB_USER=voltway_user
DB_PASSWORD=voltway123

# Configurações de Segurança
JWT_SECRET=seu_jwt_secret_muito_seguro_aqui_2024_voltway

# Configurações de CORS
CORS_ORIGIN=http://localhost:8080

# Configurações de Log
LOG_LEVEL=info

# Configurações de Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configurações de Upload (se necessário)
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Configurações de Email (para notificações futuras)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu_email@gmail.com
SMTP_PASS=sua_senha_app

# Configurações de API Externa (para mapas futuros)
GOOGLE_MAPS_API_KEY=sua_chave_google_maps_aqui

# Configurações de Cache
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Configurações de Backup
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30
